export const  locatorz = {
      taskInput : "#taskInput",
      addBtn: ".add-btn",
}
export const array = ["Saurabh","Jayesh","Dnyanesh"]

type dragger = { [key: string]: number };

export var dictionary: dragger = {};
